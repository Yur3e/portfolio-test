{
  "name": "portfolio-fullstack",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "npm --prefix client run build",
    "start": "npm --prefix server start"
  },
  "dependencies": {
    "nodemailer": "^8.0.7"
  }
}
